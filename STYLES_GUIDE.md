# HiNote 样式开发指南

**版本：** 1.0  
**日期：** 2025-11-19  
**状态：** 单文件方案

---

## 📋 文件结构

### 主样式文件

```
styles.css          # 主样式文件（4,446 行）
```

**说明：** 由于 Obsidian 插件不支持 CSS @import，所有样式都在这一个文件中。

---

## 🎯 代码组织

### 模块划分

`styles.css` 按功能划分为以下模块：

1. **Variables & Settings** (~80 行)
   - @settings 配置
   - CSS 变量定义

2. **Base Styles** (~160 行)
   - 视图基础样式
   - Markdown 渲染修复

3. **Layout & Containers** (~200 行)
   - 主容器
   - 文件列表
   - 高亮容器

4. **Highlight Cards** (~300 行)
   - 高亮卡片
   - 高亮文本
   - 装饰条

5. **Comments & Notes** (~250 行)
   - 评论列表
   - 评论输入
   - 评论操作

6. **Search** (~200 行)
   - 搜索容器
   - 搜索输入
   - 搜索提示

7. **Export** (~200 行)
   - 导出模态框
   - 导出卡片样式

8. **Chat Window** (~250 行)
   - 聊天窗口
   - 消息样式
   - 动画效果

9. **AI Features** (~150 行)
   - AI 按钮
   - AI 下拉菜单
   - AI 加载状态

10. **Flashcards** (~1,200 行)
    - 闪卡统计
    - 闪卡容器
    - 闪卡动画
    - 分组管理

11. **Mobile Responsive** (~200 行)
    - 移动端布局
    - 响应式断点

12. **Animations** (~100 行)
    - @keyframes 定义
    - 动画效果

---

## 🛠️ 开发建议

### 1. 使用清晰的注释

**模块分隔：**
```css
/* ===================================
   MODULE NAME
   模块说明
   =================================== */
```

**功能分组：**
```css
/* === 子功能名称 === */
```

**单行说明：**
```css
/* 简短说明 */
.selector {
    /* ... */
}
```

### 2. 利用编辑器功能

**大纲视图：**
- VS Code: 左侧 "Outline" 面板
- 显示所有 CSS 选择器
- 快速跳转到目标位置

**代码折叠：**
- 折叠不需要的模块
- 专注当前编辑的部分

**搜索功能：**
- 使用 Cmd/Ctrl + F 查找选择器
- 使用 Cmd/Ctrl + Shift + F 全局搜索

### 3. 命名规范

**遵循现有的命名约定：**

- **容器：** `.highlight-xxx-container`
- **卡片：** `.highlight-card`, `.flashcard`
- **按钮：** `.highlight-xxx-button`
- **输入：** `.highlight-xxx-input`
- **图标：** `.highlight-xxx-icon`

**状态类：**
- `.active` - 激活状态
- `.selected` - 选中状态
- `.hidden` - 隐藏状态
- `.loading` - 加载状态

### 4. 避免的做法

❌ **不要使用 @import**
```css
/* 不支持 */
@import url('./other.css');
```

❌ **不要使用过于具体的选择器**
```css
/* 不好 */
.container .wrapper .item .content .text {
    /* ... */
}

/* 好 */
.item-text {
    /* ... */
}
```

❌ **不要使用 !important（除非必要）**
```css
/* 尽量避免 */
.selector {
    color: red !important;
}
```

---

## 📝 修改样式的流程

### 步骤 1：定位样式

1. 使用开发者工具（Cmd+Option+I）
2. 检查元素，找到对应的 CSS 类名
3. 在 `styles.css` 中搜索该类名

### 步骤 2：修改样式

1. 在 `styles.css` 中修改
2. 保存文件
3. 重启 Obsidian 查看效果

### 步骤 3：测试

1. 测试浅色/深色主题
2. 测试不同屏幕尺寸
3. 测试移动端（如果相关）

### 步骤 4：提交

```bash
git add styles.css
git commit -m "style: 描述你的修改"
```

---

## 🎨 常见样式修改

### 修改颜色

```css
/* 使用 CSS 变量 */
.highlight-card {
    background-color: var(--background-primary);
    border-color: var(--background-modifier-border);
    color: var(--text-normal);
}
```

**常用变量：**
- `--background-primary` - 主背景色
- `--background-secondary` - 次背景色
- `--text-normal` - 正常文本
- `--text-muted` - 次要文本
- `--text-faint` - 淡文本
- `--interactive-accent` - 强调色

### 修改间距

```css
.highlight-card {
    padding: 12px;        /* 内边距 */
    margin: 8px 0;        /* 外边距 */
    gap: 4px;             /* Flexbox 间距 */
}
```

### 修改动画

```css
/* 定义动画 */
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

/* 使用动画 */
.highlight-card {
    animation: fadeIn 0.3s ease;
}
```

---

## 🐛 调试技巧

### 1. 使用开发者工具

**查看应用的样式：**
1. 右键元素 → 检查
2. 查看 "Styles" 面板
3. 看哪些样式被应用，哪些被覆盖

**实时修改：**
1. 在开发者工具中直接修改样式
2. 看到效果后，复制到 `styles.css`

### 2. 使用注释调试

```css
/* 临时禁用某个样式 */
.selector {
    /* color: red; */
    color: blue;
}
```

### 3. 添加边框调试布局

```css
/* 临时添加，调试完删除 */
.container * {
    border: 1px solid red !important;
}
```

---

## 📚 参考资源

### Obsidian 相关

- [Obsidian CSS Variables](https://docs.obsidian.md/Reference/CSS+variables/About+CSS+variables)
- [Obsidian Plugin Development](https://docs.obsidian.md/Plugins/Getting+started/Build+a+plugin)

### CSS 资源

- [MDN CSS Reference](https://developer.mozilla.org/en-US/docs/Web/CSS)
- [CSS Tricks](https://css-tricks.com/)

---

## 🎯 最佳实践

### 1. 保持一致性

- 使用相同的命名规范
- 使用相同的间距单位（如统一使用 px 或 rem）
- 使用相同的动画时长

### 2. 考虑性能

- 避免过于复杂的选择器
- 使用 `will-change` 提示浏览器优化
- 避免频繁的重绘和重排

### 3. 考虑可维护性

- 添加注释说明复杂的样式
- 使用有意义的类名
- 将相关样式放在一起

### 4. 考虑兼容性

- 测试浅色和深色主题
- 测试不同的 Obsidian 版本
- 测试桌面和移动端

---

## 🔄 更新记录

### 2025-11-19
- 创建样式开发指南
- 确定单文件方案
- 添加开发建议和最佳实践

---

**维护者：** Kai  
**最后更新：** 2025-11-19
